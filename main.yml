name : test
on :
  push:
    branches: 
    - master
  pull_request:
      branches: 
      - master
jobs:
        checkout:
          runs-on: ubuntu-latest
          steps: 
           run:  echo "bye"

         